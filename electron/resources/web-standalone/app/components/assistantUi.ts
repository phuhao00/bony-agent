export const assistantResultCardClass =
  "rounded-2xl border border-[var(--border-subtle)] bg-[var(--card-surface)] p-5 shadow-sm";

/** @deprecated Use AssistantMarkdownPreview for agent text output — prose classes alone do not render Markdown. */
export const assistantReportProseClass =
  "max-w-none text-[color:var(--foreground)]";

export const assistantEmptyStateClass =
  "flex min-h-[min(420px,52vh)] items-center justify-center rounded-2xl border border-dashed border-[var(--border-subtle)] bg-[var(--card-surface)]/50 p-10 text-center text-sm text-[color:var(--label-secondary)]";

export const assistantComposerCardClass =
  "rounded-2xl border border-[var(--border-subtle)] bg-[var(--card-surface)] p-5 shadow-sm";
