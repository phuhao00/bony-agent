{
  "name": "ai-media-agent-mobile",
  "version": "1.0.37",
  "private": true,
  "description": "AI Media Agent iOS client — full-screen WebView shell (like Windows embedded dashboard)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "sync": "npm run build && npx cap sync ios",
    "open:ios": "npx cap open ios",
    "build:ios": "bash ./build_ios.sh"
  },
  "dependencies": {
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0",
    "@capacitor/preferences": "^6.0.3",
    "@capacitor/splash-screen": "^6.0.3",
    "@capacitor/status-bar": "^6.0.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0",
    "typescript": "^5.7.3",
    "vite": "^6.0.7"
  }
}
